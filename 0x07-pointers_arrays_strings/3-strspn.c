#include "main.h"

/**
 * _strspn - check the code
 *
 * @s: param1
 * @accept: param2
 * Return: pointer.
 */
unsigned int _strspn(char *s, char accept)
{
	unsigned int i = 0;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
